// Shared between LoginScreen (writes it when signup needs email
// confirmation before a join can be completed) and App.jsx (reads it once
// a session exists, to finish joining the household).
export const PENDING_JOIN_KEY = 'plano_money_pending_join'
